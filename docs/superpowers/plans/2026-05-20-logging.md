# 日志系统 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 引入 `log` + `flexi_logger` 日志系统，按天轮转日志文件，替换分散的 `eprintln!` 调用

**Architecture:** 新增 `src/logging.rs` 作为日志初始化入口；`main.rs` 启动时优先调用；`lorebook/entry.rs` 的 `eprintln!` 迁移为 `log::warn!`；`llm.rs` 新增关键节点的 debug/info 日志

**Tech Stack:** `log` 0.4、`flexi_logger` 0.29

---

## 文件变更预览

| 文件 | 操作 | 职责 |
|------|------|------|
| `Cargo.toml` | 修改 | 新增 `log` + `flexi_logger` 依赖 |
| `src/logging.rs` | 创建 | `init()` 函数：配置 flexi_logger，按天轮转 |
| `src/main.rs` | 修改 | `mod logging;` + 第一行调用 `logging::init()?;` |
| `src/lorebook/entry.rs` | 修改 | 2 处 `eprintln!` → `log::warn!` |
| `src/llm.rs` | 修改 | 新增 `log::debug!` / `log::info!` |

---

### Task 1: 添加依赖

**Files:**
- Modify: `Cargo.toml:7-20`

- [ ] **Step 1: 在 Cargo.toml 的 `[dependencies]` 末尾追加两个依赖**

```toml
log = "0.4"
flexi_logger = "0.29"
```

用 edit 工具在 `anyhow = "1"` 后面追加：

```toml
log = "0.4"
flexi_logger = "0.29"
```

- [ ] **Step 2: 验证 Cargo.toml 格式正确**

Run: `cargo check`
Expected: 下载新依赖后编译通过（可能有未使用的 warn，正常）

---

### Task 2: 创建日志初始化模块

**Files:**
- Create: `src/logging.rs`

- [ ] **Step 1: 创建 `src/logging.rs`**

```rust
use anyhow::Context;
use std::path::Path;

/// 初始化日志系统
///
/// 日志写入 `data/logs/` 目录，按天轮转，保留最近 7 天。
/// 默认级别通过环境变量 `RUST_LOG` 控制，未设置则使用 `info,rust_sillytavern=debug`。
pub fn init() -> anyhow::Result<()> {
    let log_dir = Path::new("data").join("logs");
    std::fs::create_dir_all(&log_dir)
        .with_context(|| format!("无法创建日志目录: {}", log_dir.display()))?;

    flexi_logger::Logger::try_with_env_or_str("info,rust_sillytavern=debug")
        .with_context(|| "日志级别解析失败")?
        .log_to_file(
            flexi_logger::FileSpec::default()
                .directory(&log_dir)
                .basename("tavern"),
        )
        .rotate(
            flexi_logger::Criterion::Age(flexi_logger::Age::Day),
            flexi_logger::Naming::Timestamps,
            flexi_logger::Cleanup::KeepLogFiles(7),
        )
        .start()
        .with_context(|| "日志系统启动失败")?;

    log::info!("日志系统已初始化，日志目录: {}", log_dir.display());
    Ok(())
}
```

- [ ] **Step 2: 验证编译**

Run: `cargo check`
Expected: 编译通过

---

### Task 3: 在 main.rs 中接入

**Files:**
- Modify: `src/main.rs:1-8`

- [ ] **Step 1: 添加 `mod logging;` 声明**

在 `src/main.rs` 的 `mod` 声明区域（第 1-8 行），追加一行：

```rust
mod logging;
```

最终效果：

```rust
mod character;
mod command;
mod config;
mod conversation;
mod db;
mod llm;
mod logging;
mod lorebook;
mod tui;
```

- [ ] **Step 2: 在 `main()` 函数开头调用 `init()`**

在 `#[tokio::main]` 的 `async fn main()` 函数体第一行，调用 `logging::init()?;`：

```rust
#[tokio::main]
async fn main() -> anyhow::Result<()> {
    logging::init()?;

    let args = Args::parse();
    // ... 其余不变
}
```

- [ ] **Step 3: 验证编译**

Run: `cargo check`
Expected: 编译通过

---

### Task 4: 迁移 lorebook/entry.rs 的 eprintln!

**Files:**
- Modify: `src/lorebook/entry.rs:72,77`

- [ ] **Step 1: 将第 72 行的 `eprintln!` 替换为 `log::warn!`**

旧：

```rust
eprintln!("Warning: failed to parse {}: {}", path.display(), e);
```

新：

```rust
log::warn!("无法解析 {}: {}", path.display(), e);
```

- [ ] **Step 2: 将第 77 行的 `eprintln!` 替换为 `log::warn!`**

旧：

```rust
eprintln!("Warning: failed to read {}: {}", path.display(), e);
```

新：

```rust
log::warn!("无法读取 {}: {}", path.display(), e);
```

- [ ] **Step 3: 验证编译**

Run: `cargo check`
Expected: 编译通过

---

### Task 5: 在 llm.rs 添加关键节点日志

**Files:**
- Modify: `src/llm.rs`

- [ ] **Step 1: 在 `chat_with_messages` 函数的请求发送前添加 debug 日志**

在 `src/llm.rs` 的 `chat_with_messages` 函数中，在 `let resp = client.post(&url)...` 之前添加：

```rust
log::debug!("LLM 非流式请求: url={}, model={}, messages={}", url, config.model, messages.len());
```

- [ ] **Step 2: 在 `stream_impl` 函数开头添加 debug 日志**

在 `stream_impl` 函数体开头（约第 201 行之后），`let url = ...` 行之后添加：

```rust
log::debug!("LLM 流式请求: url={}, model={}, messages={}", url, config.model, messages.len());
```

- [ ] **Step 3: 在流式完成时添加 info 日志**

在 `stream_impl` 的两处 `let _ = tx.send(StreamEvent::Done(full_text));` 之前，分别添加：

```rust
log::info!("LLM 流式完成, 共计 {} 字", full_text.chars().count());
```

（`stream_impl` 中共有 2 处 `StreamEvent::Done` 发送，约在第 261 行和第 319 行，均添加这行日志）

- [ ] **Step 4: 验证编译**

Run: `cargo check`
Expected: 编译通过，无 warning 残留

---

### Task 6: 最终验证与提交

**Files:** 无新增

- [ ] **Step 1: 完整编译检查**

Run: `cargo check`
Expected: 零 warning、零 error

- [ ] **Step 2: 确认日志目录不在 git 追踪中**

Run: `git status`
Expected: 没有 `data/logs/` 出现在未追踪文件列表中（因为 `data/` 已在 `.gitignore`）

- [ ] **Step 3: 提交**

```bash
git add Cargo.toml Cargo.lock src/logging.rs src/main.rs src/lorebook/entry.rs src/llm.rs
git commit -m "feat: 引入 log + flexi_logger 日志系统，按天轮转文件输出"
```
